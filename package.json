{
  "name": "next-bus",
  "version": "0.2.0",
  "private": true,
  "description": "Live next-bus arrivals at the nearest Israeli bus stop — a fully client-side static app, no key required.",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "build:stops": "node scripts/build-stops.mjs"
  }
}
